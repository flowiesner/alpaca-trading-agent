services:
  trading-bot:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./journal.db:/app/journal.db
      - ./trading.log:/app/trading.log
    environment:
      - TZ=Europe/Vienna
    dns:
      - 8.8.8.8
      - 1.1.1.1
